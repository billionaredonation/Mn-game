javascript import { register, show } from '../../src/router.js'; import { state, save } from '../../src/state.js'; register('welcome2', (root) => { root.className = 'page welcome2'; root.innerHTML = `<h2>Введи никнейм</h2> <input id="nick" maxlength="16" placeholder="Ваш ник" /> <button class="btn">Далее</button>`; const inp = root.querySelector('#nick'); root.querySelector('.btn').onclick = () => { const nick = inp.value.trim(); if (!/^[A-Za-zА-Яа-я0-9_]{3,16}$/.test(nick)) { alert('Ник 3-16 символов'); return; } state.nickname = nick; save(); show('welcome3'); }; });
